name: CI

on:
  push:
    paths-ignore:
      - 'openapi/**'
      - '**.md'
    branches:
      - master
      - 'release/*'
    tags:
      - 'v*'
  pull_request:
    branches:
      - master

defaults:
  run:
    shell: bash

jobs:
  #  docker:
  #    runs-on: ubuntu-20.04
  #    env:
  #      DOCKER_REGISTRY_URL: tip-tip-wlan-cloud-ucentral.jfrog.io
  #      DOCKER_REGISTRY_USERNAME: ucentral
  #    steps:
  #    - uses: actions/checkout@v2
  #
  #    - name: Build Docker image
  #      run: docker build -t wlan-cloud-owgw:${{ github.sha }} .
  #
  #    - name: Tag Docker image
  #      run: |
  #        TAGS="${{ github.sha }}"
  #
  #        if [[ ${GITHUB_REF} == "refs/heads/"* ]]
  #        then
  #          CURRENT_TAG=$(echo ${GITHUB_REF#refs/heads/} | tr '/' '-')
  #          TAGS="$TAGS $CURRENT_TAG"
  #        else
  #          if [[ ${GITHUB_REF} == "refs/tags/"* ]]
  #          then
  #            CURRENT_TAG=$(echo ${GITHUB_REF#refs/tags/} | tr '/' '-')
  #            TAGS="$TAGS $CURRENT_TAG"
  #          else # PR build
  #            CURRENT_TAG=$(echo ${GITHUB_HEAD_REF#refs/heads/} | tr '/' '-')
  #            TAGS="$TAGS $CURRENT_TAG"
  #          fi
  #        fi
  #
  #        echo "Result tags: $TAGS"
  #
  #        for tag in $TAGS; do
  #          docker tag wlan-cloud-owgw:${{ github.sha }} ${{ env.DOCKER_REGISTRY_URL }}/owgw:$tag
  #        done
  #
  #    - name: Log into Docker registry
  #      if: startsWith(github.ref, 'refs/tags/') || startsWith(github.ref, 'refs/pull/') || github.ref == 'refs/heads/master'
  #      uses: docker/login-action@v1
  #      with:
  #        registry: ${{ env.DOCKER_REGISTRY_URL }}
  #        username: ${{ env.DOCKER_REGISTRY_USERNAME }}
  #        password: ${{ secrets.DOCKER_REGISTRY_PASSWORD }}
  #
  #    - name: Push Docker images
  #      if: startsWith(github.ref, 'refs/tags/') || startsWith(github.ref, 'refs/pull/') || github.ref == 'refs/heads/master'
  #      run: |
  #        docker images | grep ${{ env.DOCKER_REGISTRY_URL }}/owgw | awk -F ' ' '{print $1":"$2}' | xargs -I {} docker push {}

  #  trigger-testing:
  #    if: startsWith(github.ref, 'refs/tags/') || startsWith(github.ref, 'refs/pull/')
  #    runs-on: ubuntu-latest
  #    needs: docker
  #    steps:
  #    - name: Trigger testing of OpenWifi Docker Compose deployment
  #      uses: peter-evans/repository-dispatch@v1
  #      with:
  #        token: ${{ secrets.WLAN_TESTING_PAT }}
  #        repository: Telecominfraproject/wlan-testing
  #        event-type: openwifi-sdk-pr
  #        client-payload: '{"owgw_tag": "${{ github.sha }}", "owgwui_tag": "main", "owsec_tag": "main", "owfms_tag": "main", "owprov_tag": "main", "owprovui_tag": "main"}'

  trigger-testing:
    if: startsWith(github.ref, 'refs/tags/') || startsWith(github.ref, 'refs/pull/')
    runs-on: ubuntu-latest
    env:
      WLAN_TESTING_PAT: ${{ secrets.WLAN_TESTING_PAT }}
    steps:
    - name: Check Github API response with manual curl request
      run: |
        curl -i -X POST \
          -H "Accept: application/vnd.github.v3+json" \
          -H "Authorization: token ${{ WLAN_TESTING_PAT }}" \
          --data '{"event_type": "openwifi-sdk-pr", "client_payload": {"owgw_tag": "master", "owgwui_tag": "main", "owsec_tag": "main", "owfms_tag": "main", "owprov_tag": "main", "owprovui_tag": "main"}}' \
          https://api.github.com/repos/${{ github.repository_owner }}/wlan-testing/dispatches
